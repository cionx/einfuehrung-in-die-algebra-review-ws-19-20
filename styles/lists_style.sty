% settings for standard lists
% all unindented


\setlist[enumerate]   { wide          = 0pt,
                        leftmargin    = *,
                        listparindent = \parindent,
                        parsep        = 0pt }
\setlist[enumerate, 1]{ label         = \arabic*) }
\setlist[enumerate, 2]{ label         = \alph*) }


\setlist[itemize]    { wide          = 0pt,
                       leftmargin    = *,
                       listparindent = \parindent,
                       parsep        = 0pt }
\setlist[itemize, 1]{ label          = {\textbullet} }
\setlist[itemize, 2]{ label          = {\textopenbullet} }


\setlist[description]{ wide          = 0pt,
                       listparindent = \parindent,
                       parsep        = 0pt }





% indented versions of the standard lists

\newlist{enumerate*}{enumerate}{2}
\newlist{itemize*}{itemize}{2}
\newlist{description*}{description}{2}


\setlist[enumerate*]   { wide          = 0pt,
                         leftmargin    = *,
                         labelindent   = \parindent,
                         listparindent = \parindent,
                         parsep        = 0pt }
\setlist[enumerate*, 1]{ label         = \arabic*)}
\setlist[enumerate*, 2]{ label         = \alph*)}


\setlist[itemize*]     { wide          = 0pt,
                         leftmargin    = *,
                         labelindent   = \parindent,
                         listparindent = \parindent,
                         parsep        = 0pt }
\setlist[itemize*, 1]  { label         = {\textbullet} }
\setlist[itemize*, 2]  { label         = {\textopenbullet} }


\setlist[description*]{ listparindent = \parindent,
                               parsep = 0pt }





% lists for equivalences (statement) and implications (proof)

\newlist{equivlist}{enumerate}{2}
\newlist{implist}{description}{2}


\setlist[equivlist]   { wide          = 0pt,
                        leftmargin    = *,
                        labelindent   = \parindent,
                        listparindent = \parindent,
                        parsep        = 0pt }
\setlist[equivlist,1] { label = \roman*) }
\setlist[equivlist,2] { label = \alph*) }


\setlist[implist]{ wide          = 0pt,
                   font          = \normalfont,
                   listparindent = \parindent,
                   parsep        = 0pt }

% macros to work with implist


\newcommand{\textimp}[2]{\ref{#1}~$\implies$~\ref{#2}}
\newcommand{\textiff}[2]{\ref{#1}~$\iff$~\ref{#2}}
\newcommand{\impitem}[2]{\item[\textimp{#1}{#2}]}
\newcommand{\iffitem}[2]{\item[\textiff{#1}{#2}]}



